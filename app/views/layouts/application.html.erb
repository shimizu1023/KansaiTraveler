<!DOCTYPE html>
<html>
  <head>
    <title>KansaiTraveler</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header">
      <nav class="site-nav">
        <%= link_to t("nav.home"), root_path, class: "nav-link" %>
        <% if user_signed_in? %>
          <span class="nav-text"><%= current_user.display_name %></span>
          <%= link_to t("nav.new_post"), new_post_path, class: "nav-link" %>
          <%= button_to t("nav.logout"), destroy_user_session_path, method: :delete, class: "nav-button" %>
        <% else %>
          <%= link_to t("nav.login"), new_user_session_path, class: "nav-link" %>
          <%= link_to t("nav.signup"), new_user_registration_path, class: "nav-link" %>
        <% end %>
      </nav>
    </header>

    <%= yield %>
  </body>
</html>
